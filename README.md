# Instructions
- Remember to change the ports in Dockerfile,docker-compose and mailn.py ( have the same port every where )
- In the .env remember to change the API key of gemini  ( current one is just a demo )
- Build it , run it, be happy.
